import SwiftUI
import MapKit
import CoreLocation

struct BView: View {
    var body: some View {
        Text("담 기")
            .font(.title)
            .foregroundColor(.black)
            .fontWeight(.bold)
            .padding()
            .frame(maxWidth: .infinity, alignment: .top)
    }
}
